# =============================================================================
# Docker Compose Configuration for LLM4Free API Server
# Provides multiple deployment scenarios and configurations
# =============================================================================

version: '3.8'

services:
  # -----------------------------------------------------------------------------
  # Main LLM4Free API Server
  # -----------------------------------------------------------------------------
  llm4free-api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - LLM4FREE_VERSION=latest
        - BUILD_DATE=${BUILD_DATE:-}
        - VCS_REF=${VCS_REF:-}
        - VERSION=${VERSION:-latest}
    image: llm4free-api:latest
    container_name: llm4free-api
    restart: unless-stopped

    # Environment configuration
    environment:
      # Server settings
      - LLM4FREE_HOST=0.0.0.0
      - LLM4FREE_PORT=8000
      - LLM4FREE_WORKERS=1
      - LLM4FREE_LOG_LEVEL=info
      # FastAPI metadata
      - LLM4FREE_API_TITLE=LLM4Free OpenAI API
      - LLM4FREE_API_DESCRIPTION=OpenAI API compatible interface for various LLM providers
      - LLM4FREE_API_VERSION=0.2.0
      - LLM4FREE_API_DOCS_URL=/docs
      - LLM4FREE_API_REDOC_URL=/redoc
      - LLM4FREE_API_OPENAPI_URL=/openapi.json
      - LLM4FREE_DATA_DIR=/app/data

      # Authentication and rate limiting settings
      # Note: These may not be fully supported by the server yet
      # Authentication is currently disabled by default in the server

      # Optional API configuration
      # - LLM4FREE_DEFAULT_PROVIDER=ChatGPT
      # - LLM4FREE_BASE_URL=/api/v1

      # Debug mode
      - LLM4FREE_DEBUG=false
      - LLM4FREE_REQUEST_LOGGING=true

      # Advanced settings
      - LLM4FREE_CORS_ORIGINS=*

    # Port mapping
    ports:
      - "${LLM4FREE_PORT:-8000}:${LLM4FREE_PORT:-8000}"

    # Volume mounts for persistence
    volumes:
      - llm4free-logs:/app/logs
      - llm4free-data:/app/data

    # Override default command for enhanced auth system
    command: ["python", "-m", "llm4free.server.server"]

    # Health check configuration
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/monitor/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

    # Resource limits
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M
          cpus: '0.5'

    # Security settings
    security_opt:
      - no-new-privileges:true
    read_only: false  # Set to true for maximum security, but may need adjustments

    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # -----------------------------------------------------------------------------
  # Production setup with Gunicorn (alternative configuration)
  # -----------------------------------------------------------------------------
  llm4free-api-production:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - LLM4FREE_VERSION=latest
    image: llm4free-api:latest
    container_name: llm4free-api-prod
    restart: unless-stopped
    profiles:
      - production

    environment:
      - LLM4FREE_HOST=0.0.0.0
      - LLM4FREE_PORT=8000
      - LLM4FREE_WORKERS=4  # More workers for production
      - LLM4FREE_LOG_LEVEL=warning
      - LLM4FREE_DEBUG=false
      - LLM4FREE_DATA_DIR=/app/data

      # Authentication settings (production defaults)
      # Note: These may not be fully supported by the server yet
      # Server currently defaults to no authentication

      # Security and performance
      - LLM4FREE_CORS_ORIGINS=*
      - LLM4FREE_REQUEST_LOGGING=true

    ports:
      - "${LLM4FREE_PORT:-8000}:${LLM4FREE_PORT:-8000}"

    volumes:
      - llm4free-logs:/app/logs
      - llm4free-data:/app/data

    command: [
      "gunicorn",
      "--bind", "0.0.0.0:${LLM4FREE_PORT:-8000}",
      "--workers", "4",
      "--worker-class", "uvicorn.workers.UvicornWorker",
      "--log-level", "warning",
      "--access-logfile", "/app/logs/access.log",
      "--error-logfile", "/app/logs/error.log",
      "--preload",
      "llm4free.server.server:create_app"
    ]

    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '2.0'
        reservations:
          memory: 1G
          cpus: '1.0'

  # -----------------------------------------------------------------------------
  # Development setup with hot reload
  # -----------------------------------------------------------------------------
  llm4free-api-dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder  # Use builder stage for development
      args:
        - LLM4FREE_VERSION=latest
    image: llm4free-api:dev
    container_name: llm4free-api-dev
    restart: unless-stopped
    profiles:
      - development

    environment:
      - LLM4FREE_HOST=0.0.0.0
      - LLM4FREE_PORT=8000
      - LLM4FREE_WORKERS=1
      - LLM4FREE_LOG_LEVEL=debug
      - LLM4FREE_DEBUG=true
      - LLM4FREE_DATA_DIR=/app/data

      # Development settings
      # Note: Authentication settings may not be fully supported by the server yet
      - LLM4FREE_CORS_ORIGINS=*
      - LLM4FREE_REQUEST_LOGGING=true

    ports:
      - "${LLM4FREE_PORT:-8000}:${LLM4FREE_PORT:-8000}"

    volumes:
      - .:/app  # Mount source code for development
      - llm4free-logs:/app/logs
      - llm4free-data:/app/data

    command: [
      "uvicorn",
      "--host", "0.0.0.0",
      "--port", "${LLM4FREE_PORT:-8000}",
      "--reload",
      "--log-level", "debug",
      "llm4free.server.server:create_app"
    ]

  # -----------------------------------------------------------------------------
  # Nginx reverse proxy (optional)
  # Note: You'll need to provide your own nginx.conf file
  # -----------------------------------------------------------------------------
  nginx:
    image: nginx:alpine
    container_name: llm4free-nginx
    restart: unless-stopped
    profiles:
      - nginx
      - production

    ports:
      - "80:80"
      - "443:443"

    volumes:
      # Mount your own nginx configuration file
      # - ./nginx.conf:/etc/nginx/nginx.conf:ro
      # - ./ssl:/etc/nginx/ssl:ro
      - llm4free-logs:/var/log/nginx

    depends_on:
      - llm4free-api

    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/monitor/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # -----------------------------------------------------------------------------
  # Monitoring with Prometheus (optional)
  # Note: You'll need to provide your own prometheus.yml configuration file
  # -----------------------------------------------------------------------------
  prometheus:
    image: prom/prometheus:latest
    container_name: llm4free-prometheus
    restart: unless-stopped
    profiles:
      - monitoring

    ports:
      - "9090:9090"

    volumes:
      # Mount your own prometheus configuration file
      # - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus

    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'

# =============================================================================
# Named volumes for data persistence
# =============================================================================
volumes:
  llm4free-logs:
    driver: local
  llm4free-data:
    driver: local
  prometheus-data:
    driver: local

# =============================================================================
# Networks (optional, for advanced setups)
# =============================================================================
networks:
  default:
    name: llm4free-network
    driver: bridge