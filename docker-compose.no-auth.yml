# =============================================================================
# Docker Compose Override for No-Auth Mode
# Use with: docker-compose -f docker-compose.yml -f docker-compose.no-auth.yml up
# =============================================================================

version: '3.8'

services:
  # -----------------------------------------------------------------------------
  # Override main service for no-auth mode
  # -----------------------------------------------------------------------------
  llm4free-api:
    environment:
      # Enable no-auth mode for development/demos
      # Note: These environment variables may not be fully supported by the server yet
      # Server currently defaults to no authentication
      - LLM4FREE_LOG_LEVEL=debug
      - LLM4FREE_DEBUG=true
      - LLM4FREE_REQUEST_LOGGING=true
      # FastAPI metadata
      - LLM4FREE_API_TITLE=LLM4Free OpenAI API
      - LLM4FREE_API_DESCRIPTION=OpenAI API compatible interface for various LLM providers with enhanced authentication
      - LLM4FREE_API_VERSION=0.2.0
      - LLM4FREE_API_DOCS_URL=/docs
      - LLM4FREE_API_REDOC_URL=/redoc
      - LLM4FREE_API_OPENAPI_URL=/openapi.json

    # Add labels for easy identification
    labels:
      - "llm4free.mode=no-auth"
      - "llm4free.description=No authentication required - development/demo mode"

  # -----------------------------------------------------------------------------
  # Override production service for no-auth mode (not recommended)
  # -----------------------------------------------------------------------------
  llm4free-api-production:
    environment:
      # WARNING: Only use no-auth in production if you understand the security implications
      # Note: These environment variables may not be fully supported by the server yet
      - LLM4FREE_LOG_LEVEL=info
      - LLM4FREE_REQUEST_LOGGING=true

    labels:
      - "llm4free.mode=no-auth-production"
      - "llm4free.warning=No authentication - use with caution in production"

  # -----------------------------------------------------------------------------
  # Override development service for no-auth mode
  # -----------------------------------------------------------------------------
  llm4free-api-dev:
    environment:
      # Perfect for development
      # Note: Authentication settings may not be fully supported by the server yet
      - LLM4FREE_LOG_LEVEL=debug
      - LLM4FREE_DEBUG=true
      - LLM4FREE_REQUEST_LOGGING=true

    labels:
      - "llm4free.mode=no-auth-dev"
      - "llm4free.description=Development mode with no authentication"
